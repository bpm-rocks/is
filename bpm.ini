[package]
name=is
version=1.0.0
include=libis
description=Functions to determine properties of variables and functions. Is this set? Is that a function?
license=MIT

[devDependencies]
embed-docs=*
unittest=*

[scripts]
readme=embed-docs README.md libis
test=unittest test/*.test
